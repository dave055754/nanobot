#### loop
- run
```
1. 接受用户输入:msg
2. 异步执行消息处理: syncio.create_task(self._dispatch(msg))
```
- _dispatch
```
0. 加锁实现同一时间只有一个消息在处理: async with self._processing_lock:
1. 异步处理消息 _process_message(msg)
2. 等待执行完成
3. 异步发送响应回消息总线 
4. 等待执行完成
```

- _process_message(msg)
    - 如果通道是:system
        - _run_agent_loop
        - _save_turn
        - sessions.save

- self.bus.publish_outbound(response)